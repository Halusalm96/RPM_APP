LOCAL_PATH := $(call my-dir)

include $(CLEAR_VARS)
OPENCV_CAMERA_MODULES := on
OPENCV_EXTRA_MODULES += /path/to/opencv/modules/dnn/misc/onnx/opencv-onnx.cmake
OPENCV_LIB_TYPE := STATIC
include /path/to/opencv/sdk/native/jni/OpenCV.mk